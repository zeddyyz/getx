name: get
description: High performance state management and intelligent dependency injection with GetX.
version: 5.0.1
homepage: https://github.com/jonataslaw/getx

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
